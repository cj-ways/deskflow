/**
 * CLI handler: deskflow snapshot <profile-name>
 * Captures current desktop snapshot, saves as named profile, exits.
 * Full implementation in phase I2.
 */

export async function handleCliSnapshot(_name: string): Promise<number> {
  process.stderr.write('CLI snapshot not yet implemented\n')
  return 1
}
